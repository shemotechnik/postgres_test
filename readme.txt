1) схема БД в файле scheme.sql
2) вначале присутствует удаление таблиц
3) IF NOT EXISTS присутствует
4) пакет использующий pgx разработан
5) CRUD реализован методами
    Tasks
    NewTask
    UpdateTask
    DeleteTask
  другие методы реализуют ровно то, что просят в задании
6) Все методы являются методами структуры Storage, которая так же включает в себя подключение к БД
    // Хранилище данных.
    type Storage struct {
    	db *pgxpool.Pool
    }